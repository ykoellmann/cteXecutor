<idea-plugin>
    <id>com.ykoellmann.ctexecutor.ctexecutor</id>
    <name>cteXecutor</name>

    <!-- 
        Version, description and change-notes will be automatically patched 
        from PluginInfo.kt during build via build.gradle.kts 
        
        You don't need to manually update these!
    -->

    <vendor email="yankoe@outlook.de">Yannik Köllmann</vendor>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.database</depends>

    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="cteExecutor.notifications"
                           displayType="BALLOON"/>
        <applicationService serviceImplementation="com.ykoellmann.ctexecutor.notification.UpdateNotificationService"/>
        <postStartupActivity implementation="com.ykoellmann.ctexecutor.notification.UpdateNotificationStartupActivity"/>
    </extensions>

    <!-- Nur noch 2 Actions (HANDOFF.md Abschnitt 27): CTE-Modus (ExecuteFromHereAction) und
         Graph-Modus (NavigateDependencyGraphAction). CopyAction/CopyAndEditAction wurden entfernt -
         ihre Funktion (Kopieren/Einfuegen) ist jetzt E/C-Tastenkuerzel innerhalb BEIDER Popups. -->
    <actions>
        <!-- Falls bereits eine Editor-Selektion besteht, fuehrt die Action diese direkt aus (wie die
             Standard-Ausfuehrung) statt das CTE-Abhaengigkeits-Popup zu zeigen, siehe ExecuteFromHereAction. -->
        <action id="com.ykoellmann.ctexecutor.ExecuteFromHereAction"
                class="com.ykoellmann.ctexecutor.actions.ExecuteFromHereAction"
                text="Execute from Here"
                description="Execute, edit, or copy SQL from the current position with automatic CTE dependency resolution">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl NUMBER_SIGN" second-keystroke="ENTER"/>
        </action>

        <action id="com.ykoellmann.ctexecutor.NavigateDependencyGraphAction"
                class="com.ykoellmann.ctexecutor.actions.NavigateDependencyGraphAction"
                text="Navigate Dependency Graph"
                description="Navigate CTE/subquery dependencies and consumers from the current position">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl NUMBER_SIGN" second-keystroke="D"/>
        </action>
    </actions>

</idea-plugin>